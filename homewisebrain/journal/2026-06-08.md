# 2026-06-08

### [20:32] MLS go-live readiness review — non-coding config steps (branch: develop · commits: none)
**Synopsis:** Reviewed the shipped MLS foundation against the user's question "what do I configure outside of coding to make MLS listings + search work." Produced an operational checklist AND surfaced code gaps that block go-live despite config.
**What worked:** Traced the full path: `mls-sync.ts` (15-min Inngest cron, gated by `hasCredentials()`) → `mls-grid.ts` (filters `ListOfficeMlsId` when `MLS_OFFICE_ID` set) → Prisma `Listing` → `StellarMlsProvider`/`FeaturedListingsWidget`(office)/`AgentListingsWidget`(agent.`mlsAgentId`↔`listingAgentMlsId`). Cross-checked against `docs/temp/` research+setup docs. Evidence: manual — code read of all named files + `.env.example`.
**What did NOT work / key findings:** (1) `.env.example` is MISSING the MLS Grid vars the code actually reads: `MLS_GRID_CLIENT_ID`, `MLS_GRID_CLIENT_SECRET`, `MLS_GRID_BASE_URL`, `MLS_OFFICE_ID`. (2) **Auth model mismatch** — `mls-grid.ts` does OAuth2 client_credentials against `/token`, but MLS Grid issues a STATIC bearer token (per Rob's own setup doc + research doc). Code will fail auth as written. (3) Code does not send required `OriginatingSystemName` filter, does not handle `MlgCanView=false` deletes, does not filter `MlgCanUse`=IDX, and hotlinks MLS Grid media URLs (forbidden — must self-host). All flagged in `stellar-mls-data-delivery-research.md` and confirmed in code.
**Artifacts:** No code changes. Decision surfaced to user: set `MLS_OFFICE_ID` (brokerage-only) vs unset (full IDX search). Offered next steps: draft `feature-mls-go-live` plan OR add missing vars to `.env.example`. See [[knowledge/roadmap]] (largest gap = live Stellar MLS data).
**Next / open:** Awaiting user choice. When approved, a code pass must close the 5 gaps above before credentials go live; this is the real blocker, not config.

no substantive task - nothing to record (journal housekeeping: moved entry to correct date file, removed 06-07 duplicate)

### [21:05] Drafted feature-mls-go-live plan (branch: develop · commits: none)
**Synopsis:** Wrote the implementation plan to take MLS from seed data to live Stellar IDX, per user decisions: full site-wide search (MLS_OFFICE_ID unset) + HomeWise brokerage listings as featured.
**What worked:** Used superpowers:writing-plans. Grounded the plan in verified code: vitest (`vi.hoisted`+`vi.mock("@/lib/prisma")` pattern), homepage already reads `featured:true` (so featured-by-office at sync time needs no homepage change), next.config already allows Supabase image host, `idx-disclaimer.tsx` exists. 6 phases: (1) env+schema+static-token auth, (2) cursor sync w/ MlgCanView deletes + MlgCanUse + $expand + self-continuation, (3) featured helper, (4) on-demand caching image proxy to Supabase `mls-photos` bucket, (5) public IDX read filter + attribution, (6) backfill+go-live. Evidence: manual — plan written to `.claude/plans/feature-mls-go-live.md`; cross-checked types across tasks in self-review.
**What did NOT work:** (none) — plan only, not executed.
**Key design choices:** static `MLS_GRID_TOKEN` (drop OAuth client_credentials), `MLS_GRID_ORIGINATING_SYSTEM_NAME` required filter, `HOMEWISE_OFFICE_MLS_ID` (comma-sep) drives `featured`, image proxy avoids pre-downloading millions of photos at full-feed scale, MlgCanView=false → local delete, public reads require `mlgCanUse has "IDX"`.
**Artifacts:** `.claude/plans/feature-mls-go-live.md`. Branch will be `feature/mls-go-live` (Rule 6→9). [[knowledge/roadmap]] largest gap.
**Next / open:** Awaiting user approval + execution-mode choice (subagent-driven vs inline). On approval: `/git-workflow-planning:start feature mls-go-live`, stage/push plan file (Rule 6). Verify static-token auth assumption against live MLS Grid portal on day one.

### [21:30] Verified MLS Grid docs + corrected the go-live plan (branch: develop · commits: none)
**Synopsis:** WebFetched official MLS Grid v2 docs + Best Practices Guide to confirm the integration mechanics before building, then folded two corrections into `feature-mls-go-live.md`.
**What worked:** Confirmed: bearer token is portal-generated (no runtime exchange — `MLS_GRID_TOKEN` correct, existing OAuth code wrong), `OriginatingSystemName` required every query, `@odata.nextLink` paging, `MlgCanView`/`MlgCanUse` semantics, media self-host. Evidence: docs.mlsgrid.com/api-documentation/api-version-2.0 (WebFetch 2026-06-08).
**What did NOT work / corrections:** (1) Media DOWNLOADS require `User-Agent: <token>` header — MLS Grid blocks token-less downloads since 2026-06-01; my image proxy's bare `fetch(source)` would have failed → added header. (2) `OpenHouse` is NOT `$expand`-able (only Media/Rooms/UnitTypes are; expand drops cap to 1,000/req) → changed `$expand` to Media-only, added `buildOpenHouseUrl`/`fetchOpenHousePage` + new Task 2.3 to sync OpenHouse as a separate resource by ListingId, made `ResoODataResponse<T>` generic.
**Artifacts:** `.claude/plans/feature-mls-go-live.md` (updated: decisions table, Tasks 1.2/1.4/2.1/2.2, new 2.3, Phase 4 route, self-review, risks).
**Next / open:** Still awaiting approval + execution-mode choice. Not building. Could distill a `skill-integrations-mls-grid-reso` once implemented + verified.

### [22:10] Ultracode goal-verification of the go-live plan → NO_GO → rewrote to v2 (branch: develop · commits: none)
**Synopsis:** Ran a 22-agent Workflow (ultracode) to prove/disprove that feature-mls-go-live.md covers EVERY MLS-dependent feature in the dev docs. Verdict NO_GO (36/112 covered, 5 blocking+21 high adversarial findings). Verified the top claims against code, then rewrote the plan to audit-corrected v2.
**What worked:** Workflow (5 phases: extract reqs + ground-truth docs → coverage matrix 7 groups + 4 adversarial lenses → synthesis) extracted 112 requirements and produced a coverage matrix + prioritized 18-gap list + concrete plan-edit proposals. 2.26M subagent tokens, ~24min. Verified 3 highest-severity claims with grep BEFORE editing (receiving-code-review discipline): all TRUE. Evidence: grep — (G1) 17+ files read prisma.listing directly outside provider; (G3) generate-embeddings listens `mls/listing.synced`, price-change-alerts listens `mls/listing.price-changed`, rewritten sync emitted neither; (G5) code maps reso.SchoolDistrict (non-RESO → always null). Also found admin sync route+dashboard ALREADY exist (gap #16 moot) and src/lib/supabase/admin.ts exists (use it).
**What did NOT work / 8 blocking defects in v1:** (1) IDX read filter only on ~4 of 11+ public read paths → non-IDX leak (Art 19.05/19.19); (2) CMA/AVM/market-stats run sold analytics over IDX data — needs Back Office license; (3) sync stopped emitting price-changed/synced events → dead alerts+embeddings; (4) missing Art 19.22/19.23/19.09 attribution on cards+first page; (5) schoolDistrict from non-existent RESO field; (6) cursor not persisted on paging branch + `gt` drops same-ts records; (7) mlsId keyed on ListingId||ListingKey mixes namespaces; (8) phase order — Task 2.2 imported Phase 3/4 modules → checkpoint type-check fails. Plus scale: in-memory polygon ray-cast + JS cosine over embeddings at 200k.
**Artifacts:** v1 archived `archive/plans/feature-mls-go-live-v1-preaudit.md`; rewrote `.claude/plans/feature-mls-go-live.md` → v2 (10 phases, helpers-before-sync order, universal withIdx(), BO-gating flag, full attribution, pgvector/GIN/bbox, event emission, ListingKey identity, cursor fix). Workflow output: tasks/w6stqob22.output. Self-review maps every blocking gap→phase.
**Next / open:** USER DECISION needed: Back Office feed (~$450/office/yr, separate agreement) for CMA/market-stats, or ship IDX-only with those gated OFF (v2 default). Then approve v2 + execution mode. Distill `skill-integrations-mls-grid-reso` after implementation. Consider `skill-workflow-adversarial-plan-audit` (this ultracode pattern worked well).

### [22:25] Locked BO decision + committed v2 plan to repo (branch: develop · commit: f379409)
**Synopsis:** Rob chose to skip the Back Office feed for launch (sold-analytics gated OFF). Recorded the decision in the plan and pushed it to develop.
**What worked:** Edited the decisions table + compliance callout to mark the BO skip as CONFIRMED (Rob, 2026-06-08); committed `.claude/plans/feature-mls-go-live.md` and pushed to origin/develop. Evidence: `git push` → 7fe8dad..f379409. v1 stays in local `archive/` (gitignored, so not pushed — Rule 1 satisfied on disk).
**What did NOT work:** First commit attempt used PowerShell here-string (`@'...'@`) in the Bash tool → bash parse error on parens/apostrophe; redid with multiple `-m` flags. Note: use `-m` flags or `-F file` for bash commits, not PS here-strings.
**Artifacts:** commit f379409 (plan v2, 346 insertions). Docs-only change — no app build impact.
**Next / open:** Not building per instruction. Awaiting approval of v2 + execution mode (subagent-driven vs inline).

no substantive task - nothing to record (updated STATE.md current-focus to point at the MLS go-live plan for next-session recall)
