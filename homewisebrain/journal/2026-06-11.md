# 2026-06-11

### [00:05] PR #55 merged feature→develop→main, prod deploy verified (branch: develop/main · commits: 541b62f, fedb7a3)
**Synopsis:** Completed the merge chain for the MLS go-live work: PR #55 squash-merged to develop as 541b62f, main fast-forwarded to the same sha, both pushed; Vercel develop preview and production (app.homewisefl.com) both READY on 541b62f. Detailed entry was journaled under 2026-06-10 (work straddled midnight); this entry satisfies today's gate and records the final state.
**What worked:** gh pr create/merge --squash + FF promote per [[memory]] feedback_develop_to_main_no_pause. Deploy verification via `get_project.latestDeployment` → `get_deployment` by id (list_deployments/branch-alias lookups serve stale data). Evidence: dpl_A43QqbHe (develop) and dpl_4VG9GQHm (production, alias app.homewisefl.com) readyState=READY at 541b62f.
**What did NOT work:** (none)
**Artifacts:** PR https://github.com/rspeciale0519/homewise/pull/55; develop=main=541b62f (+ fedb7a3 journal commit on develop); feature/mls-go-live retained on remote.
**Next / open:** Production code is live but publicly inert (PROPERTY_PROVIDER=mock, MLS_PUBLIC_SEARCH_ENABLED=false). Awaiting live MLS Grid credentials to flip prod launch flags. Rob's to-dos: live token, OPENAI_API_KEY, link an agent's mlsAgentId via admin UI.

### [22:25] Added MLS Agent ID field to admin agent form + public profile display (branch: develop→main · commit: 98068a4)
**Synopsis:** Rob flagged that the admin UI had no field to set an agent's MLS ID (needed to link MLS listings to agent profiles — a gap I'd assumed existed when handing him the to-do). Added it end-to-end: zod schema, create/PATCH API routes (normalized via `normalizeMlsAgentId`, same as sync-side), admin AgentForm input (uppercases as you type, helper text), edit-page initialData, and a subtle "MLS ID: <mono>" line on the public agent profile under contact info.
**What worked:** Followed the form's existing field/empty-string→null patterns exactly; PATCH uses `undefined`-passthrough so partial updates don't clobber. Evidence: `tsc --noEmit` clean, lint clean, `npx vitest run src/app/api` 22 files/113 tests pass.
**What did NOT work:** Local browser smoke of the admin form blocked — no admin session on 127.0.0.1:3100 (redirects to /login); change is type/test-verified, Rob verifies visually on next login.
**Artifacts:** src/schemas/admin-agent.schema.ts, src/app/api/admin/agents/{route,[id]/route}.ts, src/components/admin/agent-form.tsx, src/app/admin/agents/[id]/page.tsx, src/app/(marketing)/agents/[slug]/page.tsx. Commit 98068a4, develop→main FF per [[memory]] feedback_develop_to_main_no_pause.
**Next / open:** Rob: set an agent's MLS Agent ID to MFR260504162 (demo Patterson agent) to verify the agent-listings widget renders. Vercel deploy verification in flight.

### [23:15] Stale-SSG bug: agent edits invisible on public pages → revalidation fix; agent-listings widget verified on PROD (branch: develop→main · commit: f327850)
**Synopsis:** Rob set Maria Alvarez's MLS ID (saved fine — DB verified) but her public profile didn't show it. Root cause: `/agents/[slug]` is SSG (`generateStaticParams`), prerendered before the edit, with no revalidation path — admin edits never appeared until the next deploy. Fixed: admin agent create/PATCH now `revalidatePath` `/agents`, the profile, and the listings page; profile also gets `revalidate = 900` so the embedded MLS-listings section tracks the 15-min sync. Deployed to prod and verified live: maria-alvarez page shows "MLS ID: MFR260504162" + "Active Listings (View all 25)" with Patterson Realty attribution — the agent-listings widget (last untested MLS feature) is now production-verified.
**What worked:** Diagnosis order: DB row first (saved correctly) → local dev render (display code fine, fresh render) → conclusion: prod static staleness. Evidence: tsc clean, vitest src/app/api 22 files/113 tests, prod deploy dpl_2U2WKCi READY at f327850, live-page innerText contains the MLS ID + listings.
**What did NOT work:** Vercel branch-alias `get_deployment` served the previous deployment again (~stale alias propagation) — `get_project.latestDeployment` is the reliable freshness check (2nd occurrence, now a firm pattern). Listings photos on prod render as placeholders: Vercel prod lacks MLS_GRID_TOKEN so the proxy can't download/refresh uncached demo photos (cached-in-bucket ones work); resolves when prod MLS env is set.
**Artifacts:** src/app/api/admin/agents/{route,[id]/route}.ts (revalidatePath), src/app/(marketing)/agents/[slug]/page.tsx (revalidate=900). Commit f327850, develop→main FF, prod READY.
**Next / open:** Note: demo listings are now publicly visible on prod agent profiles (AgentListingsWidget queries prisma directly — not gated by MLS_PUBLIC_SEARCH_ENABLED). Pre-launch site so acceptable; revisit gating if needed before real traffic. Prod photo proxy needs MLS_GRID_TOKEN env to show listing photos.

### [09:15] Prod broken-images fix: env wiring + two real proxy bugs; blocked at the end by MLS Grid demo API outage (branch: develop→main · commits: 3b403b0, ea63de1, 5f94a63)
**Synopsis:** Rob asked to fix prod's broken listing photos. Chained through FOUR causes: (1) prod missing `MLS_GRID_TOKEN`/`MLS_GRID_ORIGINATING_SYSTEM_NAME`/`HOMEWISE_OFFICE_MLS_ID` + `MLS_GRID_BASE_URL` still pointed at the PROD API (Mar 23 value) + `MLS_IMAGE_SIGNING_SECRET` didn't match the secret that signed the DB's photo URLs → set/updated all five in Vercel via the dashboard (chrome-devtools; the env table's dropdown menus need dispatched pointer-event sequences + React native-setter writes — a11y clicks close them). (2) `SUPABASE_SERVICE_ROLE_KEY` on prod was the stale Mar 23 key ("Needs Attention") → photo cache uploads 500'd → updated to the working key. (3) REAL CODE BUG: demo media server answers expired-token URLs with **HTTP 201 + application/json** — `res.ok` passed, refresh never triggered, proxy uploaded JSON to the bucket (rejected: "mime type application/json is not supported"). Fixed: `isImageResponse()` gate (ok AND content-type image/*) before caching/serving; non-image 2xx now triggers the URL-refresh path (5f94a63). (4) After all fixes deployed: MLS Grid demo API went DOWN (503 "Topology was destroyed" on ALL queries, confirmed from local too) — refresh can't fetch fresh URLs until their service recovers. Cached photos serve fine; uncached ones self-heal on view once the API returns.
**What worked:** Full-error retrieval via the Vercel dashboard logs page in-browser (the MCP runtime-logs tool truncates messages — dashboard shows full text). Local repro of the bucket upload isolated auth-vs-mime instantly (text/plain → 415 mime rejection = bucket reachable + key valid). Evidence: prod 500→502 transition after 5f94a63 (no more JSON uploads); local `tsc`/lint clean; direct curl of expired media URL shows 201+json.
**What did NOT work:** Vercel env dialog multiline .env paste concatenates lines (fill tool) — add rows one at a time. Batch-add silently skips keys that already exist in an overlapping scope (MLS_GRID_BASE_URL) — verify each var after save. Local dev server background task died mid-session (exit 116, no error in log) — restarted + re-registered Inngest.
**Artifacts:** Vercel prod env: +MLS_GRID_TOKEN, +MLS_GRID_ORIGINATING_SYSTEM_NAME=mfrmls, +HOMEWISE_OFFICE_MLS_ID, MLS_GRID_BASE_URL→api-demo, MLS_IMAGE_SIGNING_SECRET→local value, SUPABASE_SERVICE_ROLE_KEY→current. Code: src/app/api/mls-photo/route.ts (isImageResponse gate). Monitor armed for demo-API recovery.
**Next / open:** When demo API recovers: verify photos on app.homewisefl.com/agents/maria-alvarez end-to-end. NOTE: prod now syncs MLS data via its own Inngest setup if configured — check for duplicate-sync interplay with local cron later. The `MLS_GRID_BASE_URL` change is All-Environments scope — when the REAL (non-demo) credentials arrive, set it back to https://api.mlsgrid.com/v2.
