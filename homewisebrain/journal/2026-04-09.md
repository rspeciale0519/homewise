# 2026-04-09

> Milestone entry reconstructed 2026-06-07 from git history only (pre-transcript era).

### [--:--] In-app PDF document tooling, Phases 1–5 + multi-signature (branch: feature/document-viewer · commits: 23cf5cb…30132ef, then a86c32a…fc13d60 on 04-10/12)
**Synopsis:** Built the in-app PDF document tool across five phases, then rebuilt signatures as a one-to-many multi-signature system.
**What worked:** react-pdf viewer (P1), signature system + profile (P2), annotations (P3), download/print/email export via pdf-lib (P4), drafts/favorites/recents/auto-save (P5). Multi-signature: one-to-many schema, CRUD API, draw + PNG upload with transparency check, picker popover, modal management. Signature placement with drag-to-reposition and Word-style resize handles. Evidence: phase commits merged; later polished via PRs #32/#33.
**What did NOT work:** (reconstructed from git — not captured). Visible from commits: a stale-closure bug in drag (`d4fd439`), 8 PDF-viewer bugs fixed in one pass (`5bc444f`), and Vercel serverless dep issues forcing removal of native canvas/sharp/pdfjs/dompurify (`da3ebbf`/`0e61f3b`).
**Artifacts:** `src/components/documents/`, `api/documents/*`, `Track→Course` model rename. Export flatten policy: download keeps fields editable; print/email flatten ([[memory]] feedback_pdf_export_flatten_policy).
**Next / open:** Custom-text + sign-here flag annotations added late April (PR #32/#33).
