# 2026-06-14

### [00:00] Reported last session + added read-only permission allowlist (branch: develop · commits: none)
**Synopsis:** Two non-code tasks. (1) Answered "what was the last session" by reading the brain (STATE.md + journal/2026-06-13.md) — last work was the MLS IDX/VOW/BBO compliance tiers + prod go-live + the isomorphic-dompurify lambda-500 cleanups. (2) Ran /fewer-permission-prompts: scanned 50 most-recent transcripts, extracted read-only Bash/MCP tool-call frequencies, and merged a `permissions.allow` block into `.claude/settings.json`.
**What worked:** Frequency scan via a throwaway Python parser over `~/.claude/projects/*/*.jsonl` (tool_use entries). Evidence: manual — the 14 added entries all appear >=4x in transcripts and are read-only (chrome-devtools take_snapshot/take_screenshot/wait_for/list_*/get_network_request, vercel get_project/get_deployment/get_runtime_logs/list_deployments, brave/firecrawl search, git fetch). Existing hooks block preserved; allowlist was previously empty so no de-dupe needed.
**What did NOT work:** Deliberately excluded the highest-count items as unsafe: `evaluate_script` (410x, arbitrary in-browser JS), npx/node/python/`npm run *` (arbitrary code), and click/fill/navigate/press_key (mutate browser state, not read-only). Flagged evaluate_script to Rob as an opt-in given the heavy chrome-devtools workflow.
**Artifacts:** `.claude/settings.json` (new `permissions.allow`, 14 entries). No code touched.
**Next / open:** Rob to decide whether to allowlist `evaluate_script` explicitly. STATE.md still reads as of 2026-06-12 (a day behind the 06-13 journal); consolidation flagged overdue at session start.

### [00:30] Built Claude Code automations from the recommender (branch: develop · commits: 7421162 → origin/develop)
**Synopsis:** Ran /claude-automation-recommender then "build them all". Added 3 hooks, 2 subagents, 2 user-only skills tailored to this repo's own rules; committed 8 files to develop and pushed. Skipped the .mcp.json pin (marginal value for sole-dev + Windows npx footgun + secret-leak risk for the key-bearing servers) — Rob agreed via "do what you think is best".
**What worked:** All artifacts created + validated. Evidence: settings.json passes JSON.parse; all 3 hook scripts pass `node --check`; functional pipe-tests — guard-db-commands returns deny on `prisma migrate`, ask on `db:seed`, allow on `db:push`; guard-file-size warns on an 832-line file (Windows path); lint-changed runs local eslint clean (exit 0). guard-db-commands even fired live on my own test bash command. Reused existing brain-hook conventions (stdin JSON, fail-open, exit codes); brain hooks left intact (added PreToolUse + a 2nd PostToolUse entry).
**What did NOT work:** (1) .mcp.json write BLOCKED by auto-mode classifier (MCP startup-config change needs explicit auth) — deferred; planned add was chrome-devtools only (keyless, Rule 4), deliberately NOT context7/postgres/brave/firecrawl/github since global config showed they carry API keys/conn-strings that must not be committed. (2) Native-Windows node can't resolve MSYS `/c/...` paths from `$PWD` — first size-guard test returned empty; re-tested with a `C:\\` path and it worked (real hook input is always Windows-style, so non-issue).
**Artifacts:** .claude/hooks/{guard-db-commands,guard-file-size,lint-changed}.js; .claude/settings.json (PreToolUse + PostToolUse wiring); .claude/agents/{api-route-auditor,payments-security-reviewer}.md; .claude/skills/{scaffold-api-route,release-promote}/SKILL.md. Pre-existing 450-LOC violators noted (e.g. annotation-overlay.tsx 832).
**What did NOT work (cont.):** (3) my own guard-db-commands hook BLOCKED the first `git commit` because the commit MESSAGE contained the literal "prisma migrate"/"db:push" — the v1 regex matched the trigger anywhere in the command. Fixed to anchor on a command boundary (`^|[;&|]` + optional runner npx/pnpm/yarn/npm run|exec); re-verified deny on real `npx prisma migrate`, allow on a commit message with the words, ask on `npx tsx prisma/seed-listings.ts`. Lesson: command-text guards must match invocation position, not substring.
**Artifacts (cont.):** commit 7421162 (8 files, pushed origin/develop). .mcp.json left unchanged. settings.local.json confirmed gitignored; brain journal intentionally NOT in the tooling commit.
**Security follow-up (commit 336424f → origin/develop):** Background security review flagged lint-changed.js — (a) argv flag-smuggling (file_path could pose as an ESLint flag) → pass path after a `--` end-of-options marker + reject `-`-prefixed paths; (b) untrusted binary path (input.cwd used to locate node_modules/eslint) → resolve root from CLAUDE_PROJECT_DIR only + realpathSync-verify eslint.js lives under root before exec. Re-verified syntax + clean run with CLAUDE_PROJECT_DIR set. Lesson: hooks consume harness/tool input — treat file_path + cwd as untrusted (anchor flags with `--`, don't locate executables via caller-supplied cwd).
**Next / open:** If a teammate/2nd machine ever appears, revisit pinning the full MCP set with ${ENV_VAR} refs. Pre-existing 450-LOC violators (annotation-overlay.tsx 832, etc.) will warn when next edited.

### [later] Brain consolidation — knowledge reconciled, 3 skills distilled, STATE refreshed (branch: develop · commits: none yet)
**Synopsis:** Ran the overdue consolidation. Journal-vs-git gap check clean; reconciled the stale (06-07-watermark) knowledge layer against the MLS arc; distilled 3 new skills; trimmed/refreshed STATE + index + log.
**What worked:** Evidence — `git log --since=2026-06-12` shows every commit covered by journals 06-12/13/14 (no gap). Reconciled roadmap.md + features.md: the MLS integration that 06-07 flagged as "the largest gap / seed data / no plan" is now SHIPPED + demo-proven (PR #56/#57/#58, location map, compliance tiers live) — corrected both docs, moved watermark 06-07→06-14, gap narrowed to "live non-demo Stellar creds." Distilled skills: build-vercel-jsdom-lambda (established — recurred across 4+ surfaces), integrations-mls-grid-data-tiers (provisional), build-claude-hook-input-untrusted (provisional). Skills 5→8.
**What did NOT work:** (none) — no skills were contradicted, so none deprecated. Did NOT re-audit orientation.md or superseded.md line-by-line (time-boxed; no diff evidence they're contradicted — orientation is stack/layout, stable).
**Artifacts:** STATE.md (rewritten), knowledge/{roadmap,features}.md (reconciled), index.md, log.md (consolidate entry), 3 new skills/*.md. All brain-only; no app code.
**Next / open:** Stage/commit the brain updates (STATE + knowledge + skills + 2026-06-14 journal) when ready. Live-MLS-creds remains the launch gate.
