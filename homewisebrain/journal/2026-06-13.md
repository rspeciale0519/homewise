# 2026-06-13

### [21:25] Interactive location map on listing detail — shipped (branch: develop · commits: 55590a4, 14f05c1 → main, prod READY)
**Synopsis:** Same-session continuation logged under journal/2026-06-12.md ([21:20] entry; session local time was still Jun 12 — hook clock rolled to Jun 13). Replaced the Location placeholder on the listing detail page with a real Mapbox GL map (crimson marker + address popup, nav control, cooperativeGestures; placeholder fallback when coords/token missing).
**What worked:** Reusing listing-map-panel's mapbox stack. Evidence: tsc/lint/`npx vitest run` (86 files / 553 tests)/`npm run build` all exit 0; browser-verified canvas+marker+nav in the Location section locally and on app.homewisefl.com (deployments dpl_CVeTT59… and dpl_494dFdge… both READY).
**What did NOT work:** (none)
**Artifacts:** src/components/properties/listing-location-map.tsx (new), listing-detail-location.tsx; develop=main=14f05c1.
**Next / open:** (none)

### [12:00] IDX/VOW/BBO compliance tiers for Stellar MLS audit — shipped to develop+main (branch: feature/mls-compliance-tiers · PR #58 · squash a7d196d). PROD ENV FLIP PENDING ROB APPROVAL.
**Synopsis:** Built three demonstrable, isolated MLS Grid data tiers for the Stellar MLS auditor demo, to the MLS Grid Data License Agreement + IDX Rules (2026-06-15), researched live from the MLS Grid portal (app.mlsgrid.com → docs.mlsgrid.com + the Data License Agreement & IDX Rules PDFs). Compliance map: `docs/temp/mls-grid-compliance-spec.md`.
**What worked (all browser-verified locally + isolation proven):**
- **Token superset fix:** `mls-grid.ts token()` now prefers `MLS_GRID_BO_TOKEN ?? BBO_ACCESS_TOKEN ?? MLS_GRID_TOKEN`. The BBO (broker) token returns `MlgCanUse=["BO","IDX","VOW"]` per record (vs IDX-only `["IDX"]`) — proven via demo-API probe. Without this, every incremental sync re-stamps `["IDX"]` and silently strips BO/VOW.
- **BO-flag backfill** (`scripts/backfill-bo-flags.ts`): paged the BBO Property feed (no media expand → no photo churn), updated `mlgCanUse` + close fields on existing rows. Result: 12,793 rows now BO+VOW flagged (were 0). Shared DB → prod rows flagged too.
- **BBO lit up:** market-stats aggregation sold window → trailing 12 months (standard absorption window; demo solds cluster 2026-06=290/2025-02=566 so single-month was too sparse); `/market/[city]` shows real stats (Orlando 512 active / 29 sold-12mo / $355k median) + required "data courtesy of Stellar MLS as distributed by MLS GRID as of <period>" line; CMA route gated + logs BO access (verified: 45 withBo sold comps zip 34293; bo/cma log entry created — local CMA AI step 500s w/o AI key, prod-only, pre-existing).
- **VOW tier (net-new):** `withVow()` = VOW set; `/vow` portal shows VOW Terms with affirmative "I AGREE" (no data before assent, IDX Rules §35 pattern) → expanded sold-comps search (`/api/vow/comps`, 25 comps zip 34293). `VowRegistration` model records broker-consumer relationship + terms version + ip. Logged-out `/api/vow/comps` → 401; unregistered → 403. Verified registration row + vow/search + vow/vow_register log entries.
- **Audit trail (§VIII):** `MlsAccessLog` + `logMlsAccess()`; `/admin/mls-compliance` shows per-tier data counts (12,793 each), VOW registrations (buyer email + terms + ip), and the access trail (idx/vow/bo). Verified VOW=2, BO=1 events render.
- **IDX hardening:** idx-disclaimer + mls-grid-source-line now carry exact Rule 22 sold disclaimer ("Properties displayed may be listed or sold by various participants in the MLS"), Rule 23 "Listings courtesy of Stellar MLS as distributed by MLS GRID", Rule 24 "Properties may or may not be listed by the office/agent presenting the information", Rule 29 no-scraping notice. No Rule 32 prohibited "search the MLS" language present.
- Verify: tsc 0, lint 0 errors, vitest 86 files/556 tests (+3 withVow), build — all green. Additive schema via db:push (VowRegistration, MlsAccessLog).
**What did NOT work / friction:** Auto-mode classifier (correctly) blocked TWO shared-prod actions — (1) the bulk mlgCanUse backfill (got explicit consent, then ran), (2) the **prod env flip** (ANALYTICS_BO_ENABLED=true + write BBO_ACCESS_TOKEN to Vercel prod) — STILL BLOCKED, awaiting Rob's explicit authorization since it reverses the launch-gating posture + writes a broker token to the public prod app. git-workflow-planning skills still mis-parse multi-word args.
**Artifacts:** PR #58 → develop=main=a7d196d. New: src/lib/vow.ts, src/lib/mls-access-log.ts, src/app/(marketing)/vow/*, src/app/admin/mls-compliance/page.tsx, src/app/api/vow/{register,comps}/route.ts, scripts/backfill-bo-flags.ts. docs/temp/mls-grid-compliance-spec.md (compliance map + demo narrative). New tables: VowRegistration, MlsAccessLog.
**Next / open:** PROD GO-LIVE done — see [16:40] entry below.

### [16:40] Flipped prod live for the three tiers + fixed a latent prod-only 500 (commits 0d33324, 9303eac → main; Vercel prod READY)
**Synopsis:** Rob authorized the prod flip. Set Vercel prod `ANALYTICS_BO_ENABLED=true` + `BBO_ACCESS_TOKEN` (so future prod syncs keep BO/VOW flags), redeployed. Hit a prod-only 500 on `/market/[city]` (and pre-existing `/learn`): "Failed to load external module" — `isomorphic-dompurify` lazy-requires jsdom at server module-load, which Vercel's lambda file-tracer drops (local `next start` was fine since it has full node_modules). `serverExternalPackages` did NOT fix it under Turbopack. Real fix: sanitize SEO HTML browser-only via a new `SanitizedHtml` client component (deferred `import("isomorphic-dompurify")` inside useEffect) so it never enters the server module graph; removed the static server imports from market page + market-stats-view.
**What worked (all prod-verified on app.homewisefl.com):** Vercel CLI env (rm+add) + auto-deploy on main push. After the dompurify fix: `/market/orlando` 200 with real stats (512 active / 29 sold-12mo / $355k median) + source/period attribution; `/vow` logged-in shows registered comps search (e2e-buyer pre-registered via shared DB) and `/api/vow/comps?zip=34293` → 200/25 comps; logged-out `/api/vow/comps` → 401; `/admin/mls-compliance` shows tiers + access events (VOW=3, BO=1) + the buyer registration. Evidence: tsc/lint 0/vitest 556/build green on each commit; Vercel deploys polled to READY.
**What did NOT work / friction:** (1) prod-only lambda trace gap for isomorphic-dompurify — local prod build can't reproduce it (full node_modules), so it only showed on Vercel; serverExternalPackages was insufficient under Turbopack → went client-only. (2) `/learn`, training/broadcast admin views + seo-content/broadcasts API routes still import isomorphic-dompurify server-side and will 500 on Vercel the same way — PRE-EXISTING, out of this task's scope, but now diagnosed; same `SanitizedHtml`/client-only pattern fixes them.
**Artifacts:** commits 0d33324 (serverExternalPackages, kept but insufficient alone), 9303eac (SanitizedHtml client-only sanitize). New: src/components/shared/sanitized-html.tsx. Vercel prod env: ANALYTICS_BO_ENABLED=true, BBO_ACCESS_TOKEN set. Demo script: docs/temp/stellar-audit-demo-walkthrough.md.
**Next / open:** Brokerage-side compliance items (not app code): DMCA designated-agent email + §30 notice paragraphs on the site; monthly domain-usage report to MLS Grid (§VII(c)); 24h breach-notification process. Consider fixing the other isomorphic-dompurify server imports (/learn etc.) with SanitizedHtml. When live (non-demo) Stellar creds arrive, same code serves real data.
