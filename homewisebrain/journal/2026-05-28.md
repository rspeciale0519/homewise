# 2026-05-28

> Reconstructed 2026-06-07 from session transcript + git.

### [--:--] Security pass: gate training routes, lock down agent/billing leaks (branch: develop · commits: 7fe8dad)
**Synopsis:** Security review of the just-shipped Training Hub v1 found 7 issues — all auth/leak gaps in the newest feature — and fixed them.
**What worked:** Audited EVERY route variant, not just collection routes: added auth to item-level (`/api/admin/training/tracks/[id]` PATCH/DELETE — previously anyone could delete any course) and the unauthenticated signed-URL upload route. Added an explicit `select` to public `GET /api/agents` `findMany`, which had been leaking `inviteCode` (an account-claim token), `inviteExpiresAt`, email, and phone. Verified each fix live by polling the endpoint until it flipped `200 → 401` in prod (~5 min post-deploy). Evidence: commit `7fe8dad`; live 200→401 confirmation.
**What did NOT work:** N/A for the fix itself — but note the root cause pattern: the gate (`requireAdminApi`/`requireAuthApi`) was applied to collection routes and consistently missed on item/aux/upload endpoints. Also confirmed a stale code comment claiming "jsdom fails on Vercel" was wrong — `/learn` proves `jsdom@^3` works server-side.
**Artifacts:** training/agent/billing route auth; `src/lib/admin-api.ts`. [[skill-api-route-variant-auth-audit]], [[knowledge/superseded]].
**Next / open:** Apply the route-variant audit proactively when adding any future feature's routes.
