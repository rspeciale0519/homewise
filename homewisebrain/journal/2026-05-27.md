# 2026-05-27

> Reconstructed 2026-06-07 from session transcript + git.

### [--:--] Training Hub v1 — credible foundation (branch: feature/training-hub-v1 · commits: d9f303f, a72eedc · PR #51/#53)
**Synopsis:** Rebuilt the Training Hub from scratch as v1 — admin authoring, agent learner, and a public `/learn` SEO surface — over Course→Section→Content entities, replacing the 2026-03 redesign.
**What worked:** Three audiences: admin authoring (`/admin/training`, block editor), agent learner (`/dashboard/training`), public learner (`/(marketing)/learn/[slug]` with JSON-LD, OG, canonical `https://app.homewisefl.com/learn`, breadcrumbs) — treat public pieces as marketing/lead-gen, not internal collateral. Content audience flag `agent_only|public_only|both` (migrated from legacy `agent|public|both`; legacy `category` string column preserved, dropped in v2). Wired filter/drawer category selects to the dynamic `fetchCategoryOptions` loader, not hardcoded `CATEGORIES`. Shipped inert columns (`passThreshold`,`dueDays`,`recurDays`,`dripDays`) so v2 activates with no migration. Evidence: shipped PR #51 + consistency PR #53; manual E2E.
**What did NOT work:** `prisma generate` hit a Windows EPERM/DLL lock (dev server held the client) — retry cleared it, Vercel build unaffected. Reconfirmed `migrate dev` is unusable here → `db push`. E2E caught the admin "All Categories" filter still using a hardcoded const while the dynamic loader already existed.
**Artifacts:** `src/app/admin/training/`, `src/components/admin/block-editor/`, `src/lib/training/`. [[knowledge/features]], [[skill-supabase-prisma-db-push]].
**Next / open:** v2 (compliance/certificates/quizzes) and v3 (analytics/FTS/discussions) extracted as standalone plans (`269a6df`) — see [[knowledge/roadmap]].
