# 2026-06-15

### [00:00] Reviewed agent sign-up flow + planned an agent-application approval workflow (branch: develop · commits: none)
**Synopsis:** Non-code session. Mapped the existing agent sign-up flow end-to-end, established (with Rob) the missing requirement — prospective HomeWise Agents must be corporate-approved before any backend access, and those without MLS access route to Realty International USA (RIUSA) — then produced an implementation plan. No code written; plan only.
**What worked:** Three Explore-agent sweeps reconstructed the flow with concrete paths. Evidence: manual — sign-up is invite-only by design: `/register` (`src/app/(marketing)/register/page.tsx`) → `supabase.auth.signUp` in `register-form.tsx` → `/auth/callback/route.ts` creates the `UserProfile` and assigns `role` (agent only if a valid invite). Confirmed NO approval workflow exists: `Agent` model has only `active:Boolean` (no status/pending), no `AgentApplication` model, no approve/reject routes, no public application form (only ContactSubmission/HomeEvaluation/BuyerRequest at `/admin/submissions`). Identified reuse seams for the plan: `src/lib/invite-codes.ts` (`createInviteCode`, 7-day), `src/lib/email/index.ts` (`sendEmail`) + `templates.ts`, `requireAdminApi`/`isError` (`src/lib/admin-api.ts`), `generateSlug` (`src/lib/utils.ts`), and the submissions table/queue UI pattern.
**What did NOT work:** Plan-mode harness auto-named the plan `~/.claude/plans/snazzy-twirling-galaxy.md` (random name, global path) — violates project Rule 6. Rob rejected ExitPlanMode and asked to just re-save properly; rewrote to `.claude/plans/feature-agent-application-approval.md` (maps to branch `feature/agent-application-approval`). Left the global throwaway untouched (outside project tree, Rule 9).
**Artifacts:** `.claude/plans/feature-agent-application-approval.md` (4-phase plan: AgentApplication model + schemas → public `/become-an-agent` form w/ MLS branch + RIUSA interstitial → admin approval queue w/ auto-create-Agent+invite-on-approve → verification). Locked decisions: separate `AgentApplication` model; MLS self-declared at apply-time with explanatory interstitial before RIUSA redirect; approve = auto-create Agent + auto-issue invite + email link. Key security property recorded: un-approved applicant has no UserProfile/Agent row, so approval IS the gate (no new role/middleware needed).
**Next / open:** Plan not yet approved/executed. Rob to decide whether to commit+push the plan file (Rule 6 wants plans tracked). New env vars the build will need: `NEXT_PUBLIC_RIUSA_URL`, `ADMIN_NOTIFICATION_EMAIL`. On approval → `/git-workflow-planning:start feature agent-application-approval`, Phase 1 = `prisma db push` for the new model (shared prod DB — confirm first).

### [later] Built the agent-application approval feature autonomously via /autopilot → /goal (branch: feature/agent-application-approval · commits: a852c7d, f5b3a93, ba4b7e0)
**Synopsis:** Ran /autopilot on the plan → classified as /goal (build-to-done, verifiable), authored spec `docs/AGENT_APPLICATION_APPROVAL_SPEC.md` + the goal condition, then executed all 3 build phases. Public application flow + admin approval queue, reusing existing invite-code/email/admin-auth machinery. Stopped at the one owner-gated step (prod DB push) per the goal's escape hatch.
**What worked:** All static gates green. Evidence: `npx tsc --noEmit` exit 0; `npx eslint .` exit 0 (1 pre-existing docs/temp warning); `npm run build` exit 0 with all 7 new routes present (`/become-an-agent`, `/api/agent-application`, `/admin/agent-applications`(+`/[id]`), `/api/admin/agent-applications`(+`/[id]/approve`,`/reject`)); all new files <450 LOC (largest application-form.tsx 290). Key reuse: extracted `createAgentRecord` into `src/lib/agents.ts` (now shared by the agents POST route + the approve route); approve runs in `prisma.$transaction` (createAgent + invite code via `generateInviteCode`/`getInviteExpiryDate` + mark approved) then emails the `/register?invite=` link. `prisma generate` makes the AgentApplication delegate available for type-check WITHOUT db push — so full static verification ran without touching prod.
**What did NOT work:** (1) Background commit security review flagged 2 real MEDIUM issues on my own code: HTML-injection (applicant fields interpolated raw into email HTML) and host-header injection (email/invite links built from `request.nextUrl.origin`). Fixed both: added `esc()` + `subjectSafe()` in templates.ts; switched all links to trusted `process.env.NEXT_PUBLIC_SITE_URL ?? SITE_URL` (constant already existed in constants.ts). Lesson [[skill-build-claude-hook-input-untrusted]] generalizes — applicant form input is untrusted sink-side too (email HTML), and request Host is untrusted for link-building. (2) Hard stop at `prisma db push`: shared prod DB, goal required owner approval → wrote `docs/temp/agent-application-BLOCKER.md` and stopped instead of writing prod. Browser E2E also deferred (needs the table to exist).
**Artifacts:** 3 commits above (branch not pushed — pushing before the table exists would 500 the deployed routes). Spec: docs/AGENT_APPLICATION_APPROVAL_SPEC.md. Blocker: docs/temp/agent-application-BLOCKER.md. New env vars: NEXT_PUBLIC_RIUSA_URL, ADMIN_NOTIFICATION_EMAIL, NEXT_PUBLIC_SITE_URL (override). Design unchanged from plan; AgentApplication model only on this branch (not in prod schema yet).
**Best-effort E2E (chrome-devtools, dev on 127.0.0.1:3100):** verified everything that doesn't need the un-pushed table — `/become-an-agent` 200; No-MLS → intent → "MLS Access Is Required" interstitial with correct "launching soon" placeholder when NEXT_PUBLIC_RIUSA_URL unset (no row created); Yes-MLS → full form (honeypot hidden); footer CTA present; gating proven (unauth `/admin/agent-applications` → 307 /login, `/api/admin/agent-applications` → 401). Screenshots in docs/temp/. Submit→approve→register deferred (needs db push + admin session).
**Next / open:** Owner: approve + run `npm run db:push`, set env vars, run the remaining db-gated E2E (steps in the blocker file), then push branch / open PR. Approval gate enforced purely by "no account until approved" — no new role/middleware (verified: auth/callback + role gates unchanged; unauth gating proven 307/401). Dev server still running background on 127.0.0.1:3100; browser left open per Rule 4.
