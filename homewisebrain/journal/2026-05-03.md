# 2026-05-03

> Milestone entry reconstructed 2026-06-07 from git history only.

### [--:--] Direct mail ordering — HomeWise → YellowLetterShop (branch: feature/direct-mail-ordering · commits: 77e382e, b3110ac · PR #34)
**Synopsis:** Shipped the direct-mail ordering integration routing HomeWise orders to YellowLetterShop.
**What worked:** Order flow UI, `lib/direct-mail/` layer (CSV + artwork validators, signed-URL uploads, dispatch, order-summary PDF), `MailOrder`/`MailOrderDispatchLog` models, Inngest functions. Evidence: PR #34 merged + release `b3110ac`.
**What did NOT work:** (reconstructed from git — not captured).
**Artifacts:** `src/lib/direct-mail`, `src/app/dashboard/direct-mail`, `src/inngest/functions`. [[knowledge/features]].
**Next / open:** v1 only — proofing/payment/tracking are explicit v2/v3 non-goals.
