# 2026-06-12

### [13:05] MLS native feature suite: 14 features, 7 build phases, shipped to prod (branch: feature/mls-native-suite → develop → main · PR #56 · squash ffa478f)
**Synopsis:** Goal-driven run (/goal vs `.claude/plans/feature-mls-native-suite.md`) building every buildable MLS-dependent feature from the dev-docs gap analysis, natively on the direct MLS Grid connection. All 7 build phases complete with per-phase checkpoints (tsc/lint/full vitest green every time; suite grew 507→553 tests); production build green; PR #56 squash-merged to develop and promoted to main.
**What worked (per phase, all live-data verified):** P1 PriceHistory model + sync capture + recharts timeline ("Down $25,600 since listing" rendered) + TCO panel w/ real tax + HOA-frequency normalization (lib/tco). P2 compare page (3-listing table verified) + localStorage compare bar/toggles + OpenHouseRsvp model/API/form/agent-hub list (agent-match email, admin fallback, send-failure never blocks). P3 exclusive pocket listings: withIdx() → AND/OR visibility (IDX OR approved-manual; MLS compliance clause untouched), agent CRUD w/ pending-on-edit + MLS-row guards (tested), admin approval queue, Exclusive badges, manual-listing-photos bucket; verified live in search + detail. P4 commute search: Mapbox geocode+isochrone → simplified ring → existing polygon path; 9,607→259 actives within 15-min drive of downtown Orlando. P5 ListingViewDaily counters (2 views recorded via page hook), client-listing matcher (event+daily sweep; match row persisted for Maria's test contact), anomaly scan (price-drop>15%/7d via PriceHistory, DOM>180, dupes, no-photos) + admin panel. P6 weekly open-house digest (suppression + zero-match skip) + deterministic recommender feeding a recommended_listings campaign token (3 cards incl. the exclusive listing generated against real data). P7 rule-based auto-tags at sync + 9,901 rows backfilled (pool=2,321), GIN-indexed tag filter (12,794→2,321), clickable chips, gated aiStyleTags (asserted no network call without OPENAI_API_KEY), logged-in match-score badges from favorites/recents/saved-search profiles; incremental sync re-verified idle/no-error.
**What did NOT work / friction:** Prisma generate EPERM whenever the dev server holds the engine DLL → settled pattern: stop dev → db:push + generate → restart + re-register Inngest (hit 3×). Dev server holding a stale client caused two transient page 500s after schema pushes (PrismaClientValidationError) — same restart fixes. chrome-devtools MCP disconnected mid-run → switched evidence to HTTP innerHTML checks + tsx provider probes (equally transcript-provable). Discriminated-union narrowing: `"error" in owned` doesn't narrow optional-property unions → use {ok:false,response} shape.
**Deferred (documented in spec):** CRM UI for editing contact pref fields (matcher fields exist; no contact-form component found); agent/admin dashboard pages visually verified by Rob behind login; community-feature tags (golf/55+/boating) only accrue at sync since DB doesn't store CommunityFeatures; AI tagging/match-embedding upgrades await OPENAI_API_KEY.
**Artifacts:** PR https://github.com/rspeciale0519/homewise/pull/56 (75 files, +4,430). develop=main=ffa478f. New tables: PriceHistory, OpenHouseRsvp, ClientListingMatch, ListingViewDaily, ListingAnomaly (+Contact pref fields, Listing.tags/manualStatus/createdByAgentId — all additive via db:push). New Inngest jobs registered: client-listing-matcher, daily-client-match-sweep, listing-anomaly-scan, weekly-open-house-digest.
**Next / open:** Vercel prod deploy verification for ffa478f in flight; prod smoke of public pages after READY. Local dev stack left running (127.0.0.1:3100 + Inngest 8288).

### [14:10] Flipped prod launch flags — public MLS search is LIVE on app.homewisefl.com (no commit; env + redeploy)
**Synopsis:** Rob asked why home-page search returned nothing on prod (mock provider, launch gate) and then approved going live against demo data. Set Vercel Production `PROPERTY_PROVIDER=stellar` and `MLS_PUBLIC_SEARCH_ENABLED=true`, redeployed, verified.
**What worked:** Vercel CLI route after chrome-devtools MCP stayed dead: `npm i -g vercel` → already authenticated as rspeciale0519 (no login needed!) → `vercel env rm/add <var> production` → `vercel redeploy app.homewisefl.com` (Ready in 3m, aliased). Evidence: prod /properties serves 12 real listing cards (24 "Listing #MFR" attributions) with total 12,794 in payload; detail page shows real address + Price History + TCO + RSVP. Note: "properties found" phrase is client-rendered (filters component) — absent from SSR HTML; count value lives in the RSC payload.
**What did NOT work:** chrome-devtools MCP cannot reconnect mid-session (ToolSearch confirms gone) — Vercel CLI is the better env-management path anyway; record as the standing method.
**Artifacts:** Vercel prod env: PROPERTY_PROVIDER=stellar (now Production-scope only — Dev/Preview scopes dropped during rm; preview falls back to mock default, unchanged behavior), MLS_PUBLIC_SEARCH_ENABLED=true. Deployment homewise-r17re6wkm aliased to app.homewisefl.com.
**Next / open:** When REAL MLS creds arrive: swap MLS_GRID_TOKEN/BASE_URL/ORIGINATING_SYSTEM_NAME + HOMEWISE_OFFICE_MLS_ID via `vercel env`, re-import. Public site now shows demo MLS data — intended for proving; remember it's fake inventory if anyone stumbles on the site.

### [20:50] Full browser E2E smoke (no-email scope) — 100% PASS, 6 real defects found & shipped (branch: test/e2e-browser-smoke → develop → main · PR #57 · squash 726ecb7)
**Synopsis:** Goal-driven run of `.claude/plans/test-e2e-browser-smoke.md` phases A–J via chrome-devtools MCP (fresh session restored the MCP). Every in-scope test PASS with in-transcript evidence; results table in `docs/temp/e2e-smoke-results.md`. Six genuine defects surfaced by real-browser testing were fixed, merged, and verified live on prod (deployment dpl_DZaejR3LkgneCbV9Mppx45upAHL7 READY, sha 726ecb7).
**What worked:** Isolated browser contexts (`new_page isolatedContext=e2e-buyer/agent/admin/public`) gave four parallel role sessions without touching Rob's tabs — huge for multi-role e2e. Account bootstrap via `scripts/e2e-smoke-accounts.ts` (supabase admin createUser + prisma upsert; agent row active=false stays out of public directory). Inngest dev GQL `mutation { invokeFunction(functionSlug: "homewise-listing-anomaly-scan") }` triggers cron-only functions on demand (→ 37 stale-dom anomalies). RSC-payload `totalResults` regex over `fetch('/properties?...')` = fast count assertions; body.innerText prices need card-scoping (filter-option labels poison loose `\$[\d,]+` matches — caught a false PASS). Mapbox polygon Draw drivable with synthetic MouseEvents on the canvas. Evidence: type-check/lint/`npx vitest run` (86 files/553 tests)/build all exit 0 at both checkpoints; every fix re-verified in the browser; prod spot-check incl. type-filter fix (Villa=399 on app.homewisefl.com).
**What did NOT work (defects found):** (1) propertyType filter returned 0 vs MLS rows — UI sends mock-era labels, feed stores RESO PropertyType/propertySubType → synonym map in stellar provider; (2) status options "For Sale"/"New Listing" don't exist in feed → real values; (3) POST /api/user/saved-searches had NO UI caller — saved searches were uncreatable → SaveSearchButton; (4) useTrackView hook existed but was never mounted — Recently Viewed permanently empty → TrackView on detail page; (5) favorites/recently-viewed pages rendered raw cuid ids ("Property cmq8…") → server-side detail resolution w/ withIdx; (6) exclusive-listing edit 400'd — schema rejected null county/hoaFrequency that the edit form round-trips → .nullish(). Also: git-workflow-planning skills mis-parse multi-word args (type/subject split) — proceed with intended values manually.
**Artifacts:** PR https://github.com/rspeciale0519/homewise/pull/57; develop=main=726ecb7; docs/temp/e2e-smoke-results.md (full table), docs/temp/e2e-smoke-accounts.md (e2e creds); spec marked ✅ COMPLETE. Test data left (clearly labeled): e2e-* users, 901/902 E2E test listings (rejected/archived), 37 anomaly rows (real scan output, 1 dismissed).
**Next / open:** Detail-page Location section is a designed placeholder (no interactive map) — candidate enhancement, not a bug. Saved-search alert left OFF for e2e-buyer. Dev stack left running (3100 + Inngest 8288, functions registered).

### [21:20] Interactive location map on listing detail (branch: develop · commit: 55590a4 → main, prod READY)
**Synopsis:** Replaced the designed Location placeholder with a real Mapbox GL map — single crimson marker w/ address popup, nav control, `cooperativeGestures: true` (no scroll hijack on the long detail page). Graceful fallback to the old placeholder when coords or NEXT_PUBLIC_MAPBOX_TOKEN are missing.
**What worked:** Reused listing-map-panel's stack (mapbox-gl 3.19, streets-v12, token env). Evidence: tsc/lint/vitest (86 files/553 tests)/build all exit 0; browser-verified locally AND on prod (canvas+marker+nav ctrl in Location section at app.homewisefl.com, deployment dpl_CVeTT59… READY) — prod render also proves the Mapbox token is in Vercel prod env.
**What did NOT work:** (none)
**Artifacts:** src/components/properties/listing-location-map.tsx (new), listing-detail-location.tsx (placeholder swap). develop=main=55590a4.
**Next / open:** (none)
